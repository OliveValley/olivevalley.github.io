/* General Body and Container Styles */
body {
    font-family: sans-serif;
    line-height: 1.6;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
    color: #333;
}
.container {
    width: 80%;
    margin: auto;
    overflow: hidden;
    padding: 20px 0;
}

/* Header Styles */
header {
    background: #333;
    color: #fff;
    padding-top: 30px;
    min-height: 70px;
    border-bottom: #77a800 3px solid;
}
header a {
    color: #fff;
    text-decoration: none;
    text-transform: uppercase;
    font-size: 16px;
}
header ul {
    padding: 0;
    list-style: none;
}
header li {
    display: inline;
    padding: 0 20px;
}
header #branding {
    float: left;
}
header #branding h1 {
    margin: 0;
}
header nav {
    float: right;
    margin-top: 10px;
}

/* Hero Section (Homepage specific) */
.hero {
    background: #e2f0d9;
    color: #333;
    padding: 50px 0;
    text-align: center;
}

/* General Section Styles */
.section {
    padding: 20px 0;
    background: #fff;
    margin-bottom: 20px;
    border-bottom: #ddd 1px solid;
}
.section h2 {
    color: #77a800;
    text-align: center;
    margin-bottom: 30px;
}
.section-content {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
}

/* Item Specific Styles (Services, Expertise, Values, Projects) */
.service-item, .expertise-item, .value-item, .project-item {
    flex-basis: 45%; /* Adjust for two columns */
    margin-bottom: 20px;
    padding: 20px;
    background: #f9f9f9;
    border: #ddd 1px solid;
    border-radius: 5px;
}
.service-item h3, .expertise-item h3, .value-item h3 {
    color: #5cb85c;
}

/* Team Member Specific Styles */
.team-member {
    flex-basis: 30%; /* Adjust for three columns */
    margin-bottom: 20px;
    padding: 20px;
    background: #f9f9f9;
    border: #ddd 1px solid;
    border-radius: 5px;
    text-align: center;
}
.team-member img {
    max-width: 150px;
    border-radius: 50%; /* Makes image round */
    display: block;
    margin: 0 auto 15px; /* Centers image */
}

/* Contact Info & FAQ Item Styles */
.contact-info, .faq-item {
    padding: 20px;
    background: #f9f9f9;
    border: #ddd 1px solid;
    border-radius: 5px;
    margin-bottom: 15px;
}
.contact-info p {
    margin: 5px 0;
}
.faq-item h3 {
    color: #77a800;
}

/* Project Gallery & Image Styles */
.project-gallery img {
    max-width: 100%;
    height: auto;
    display: block;
    margin-bottom: 15px;
    border-radius: 5px;
}
.logo-container {
    text-align: center;
    margin-bottom: 30px;
}
.logo-container img {
    max-width: 200px;
    height: auto;
}
.image-gallery {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    gap: 20px;
    margin-top: 30px;
}
.image-gallery img {
    max-width: 45%;
    height: auto;
    border-radius: 5px;
}

/* Footer Styles */
footer {
    text-align: center;
    padding: 20px;
    background: #333;
    color: #fff;
}
